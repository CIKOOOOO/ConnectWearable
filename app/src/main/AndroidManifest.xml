<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.connectwearable">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.smartbca.registdevice.authenticatedevice.AuthenticateDeviceActivity"></activity>
        <activity android:name=".ui.smartbca.registdevice.listdevice.ListDevice" />
        <activity android:name=".ui.smartbca.main.MainSmartBCA" />
        <activity android:name=".ui.tariktunai.confirmation.ConfirmationTarikTunai" />
        <activity android:name=".ui.tariktunai.main.MainTarikTunai" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".ui.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>